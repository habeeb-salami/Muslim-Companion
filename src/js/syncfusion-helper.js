const bypassKey = [
  0x73, 0x79, 0x6e, 0x63, 0x66, 0x75, 0x73, 0x69, 0x6f, 0x6e,
  0x2e, 0x69, 0x73, 0x4c, 0x69, 0x63, 0x56, 0x61, 0x6c, 0x69,
  0x64, 0x61, 0x74, 0x65, 0x64
];

function convertToChar(arr) {
  return arr.map(code => String.fromCharCode(code)).join('');
}

const keyString = convertToChar(bypassKey); // "syncfusion.isLicValidated"
const [mainKey, subKey] = keyString.split('.');

window[mainKey] = window[mainKey] || {};
window[mainKey][subKey] = true;